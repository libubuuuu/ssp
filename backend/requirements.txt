# AI 创意平台 - 后端依赖
fastapi==0.122.1
uvicorn[standard]==0.27.1
python-multipart==0.0.26
pydantic==2.6.1
pydantic-settings==2.1.0

# 数据库
# 业务代码继续直接用 sqlite3(快、简单);alembic 仅用于 schema 迁移管理(脚手架)
# 迁 Postgres 时切 DATABASE_URL + 安装 psycopg2-binary 即可,业务代码无需改
alembic==1.18.4
SQLAlchemy==2.0.49

# Redis / 任务队列
redis==5.0.1

# HTTP 客户端
httpx==0.26.0
aiofiles==25.1.0

# 环境变量
python-dotenv==1.2.2

# 文件处理
Pillow==12.2.0

# 阿里云短信 (可选)
alibabacloud-dysmsapi20170525==3.0.0

# 认证
PyJWT==2.12.0
bcrypt==4.1.2
# 2FA / TOTP(管理员强制 2FA + 二维码)
pyotp==2.9.0
qrcode==8.2

# FAL AI
fal-client==0.5.6

# 系统监控
psutil==5.9.8

# P5: 错误监控(可选 — SENTRY_DSN 环境变量启用,留空时跳过 init)
sentry-sdk[fastapi]==2.20.0
