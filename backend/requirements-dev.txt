# 测试相关 - 开发依赖,不进生产
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
# httpx 已在 requirements.txt 里(0.26.0),用作 fastapi.testclient 后端
# ruff 用于 lint(可选,CI 里跑)
ruff==0.6.9
# pip-audit 用于扫描依赖 CVE(CI 里跑,基线降到 0 后改为强制阻塞)
pip-audit==2.10.0
