# 5/16 巡检复核(P32 一镜一图)

**触发条件:** 当天日期 ≥ 2026-05-16,用户来开会话说"看 TODO"或类似。

## 背景
- P32(commit b3fec8a)装了一镜一图巡检脚本 `/opt/ssp/deploy/p32_inspect.sh`
- cron `0 4 15 5 *` UTC,本地 2026-05-15 12:00 单次跑
- 报告输出:`/var/log/p32_inspect_<timestamp>.txt`
- cron 日志:`/var/log/p32_inspect.log`

## 复核步骤
1. `ls -lt /var/log/p32_inspect_*.txt | head -3` —— 确认 5/15 跑过,拿最新一份报告
2. `cat /var/log/p32_inspect.log | tail -50` —— 看 cron 是否成功执行,有无报错
3. 读最新 `p32_inspect_*.txt` 全文,关注 5 项指标:
   - generate 提交总次数
   - compose_first_frame_for_scene 失败次数
   - 回退共享首帧 fallback 命中(关键稳定性指标)
   - AI 带货视频超时(15 min)次数
   - **fallback 率 > 10%** 就提示加 retry
4. 如有异常 case,grep backend 日志(`/root/ssp/backend/` 或 supervisor stdout)抓真因
5. 输出简报告诉用户:绿/黄/红 三档,给出是否需要人工介入的明确判断

## 完成后
- 把这份 TODO 文件删掉(`rm /root/ssp/TODO-2026-05-16.md`),避免下次会话又触发
- 如果有后续修复动作,按 SSP 标准流程:Edit → grep 验证 → npm run build(前端)→ commit → deploy.sh
