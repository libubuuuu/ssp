# 后端测试覆盖率报告 — 2026-04-27 P7

跑命令:`cd /root/ssp/backend && venv/bin/pytest --cov=app --cov-report=term-missing`

**整体:46%**(3188 stmts, 1717 missed) 159 测试全过

## 核心路径(用户要求 >= 70%)

| 模块 | 覆盖率 | 状态 |
|---|---|---|
| `app/api/auth.py` | **89%** | ✅ 达标 |
| `app/services/billing.py` | **91%** | ✅ 达标 |
| `app/api/jobs.py` | **48%** | ❌ **不达标** |
| `app/api/payment.py` | **50%** | ❌ **不达标** |

### jobs.py 不达标分析(48%)
未覆盖主要是 `_execute_job` 异步执行路径(行 121-180):
- 真跑 FAL 的 job 流程 — 测试用 `_noop_execute_job` 替代,本质没测真扣费 + 归档 + 历史写入
- 失败回滚路径 — 同上

**补齐建议**:加 mock fal_service 的 integration 测试(2-3 小时工作量,新一轮 P 任务)

### payment.py 不达标分析(50%)
未覆盖主要是支付确认 + 退款流程(行 86-117 / 159-178 / 221-255):
- `confirm_order` 部分分支
- 充值套餐元信息接口虽有调,但管理路径未测

**补齐建议**:加 admin 确认订单 + 退款的端到端测试(1-2 小时)

## 其他模块覆盖率

### ✅ 高覆盖率(满足或接近 100%)
| 模块 | % | 备注 |
|---|---|---|
| `digital_human.py` | 100% | P0 修复全测 |
| `content_filter.py` | 100% | P2 全测 |
| `database.py` | 97% | schema 查询基本覆盖 |
| `media_archiver.py` | 96% | BUG-2 全测 + httpx mock |
| `services/auth.py` | 95% | JWT / 密码 / 创建用户全覆盖 |
| `services/billing.py` | 91% | 扣费 / 返还 / 消费记录 |
| `api/auth.py` | 89% | 注册 / 登录 / 2FA / refresh / IP 限流 |
| `config.py` | 89% | 主路径覆盖 |
| `services/audit.py` | 84% | log_admin_action 主路径 |
| `middleware/request_id.py` | 83% | trace_id 中间件 |

### ❌ 0% 覆盖(完全未测)
| 模块 | 原因 |
|---|---|
| `api/avatar.py` | 真接口刚接通,没写测试 — **下次专项补**(BUG-2 接入了归档,该补) |
| `api/products.py` | 商家产品功能未测,业务流程长 |
| `api/content.py` | 内容上传功能未测 |
| `services/feishu.py` | 飞书告警,外部 webhook 不好 mock |
| `services/health_check.py` | 自检脚本,与监控脚本重叠 |
| `app/main.py` | 主程序入口,启动代码不测正常 |

### 🟡 30-60% 覆盖(部分覆盖,有较大缺口)
| 模块 | % | 备注 |
|---|---|---|
| `services/decorators.py` | 27% | **重要**:@require_credits 装饰器主路径未单独测 |
| `services/fal_service.py` | 21% | 外部 API,mock 复杂;但单元覆盖太低 |
| `services/circuit_breaker.py` | 32% | 熔断器,失败模式没真触发 |
| `services/alert.py` | 29% | 告警发送,外部依赖 |
| `services/task_queue.py` | 31% | 文件队列,Phase 2 退役所以低优先级 |
| `api/admin.py` | 35% | 管理员功能多,只测了 adjust-credits |
| `api/image.py` | 45% | /style /realistic /multi-reference 主路径 |
| `api/jobs.py` | 48% | 见上"核心路径分析" |
| `api/payment.py` | 50% | 见上 |
| `api/tasks.py` | 53% | WS 鉴权 + 归属 + 推送测了 |
| `api/video.py` | 55% | image-to-video / replace / clone 主路径,异步推送未细测 |

## 结论

- **达标 2/4**:auth + billing 都 >=89%
- **不达标 2/4**:jobs + payment 在 48-50%(主要是异步 + 管理员路径未覆盖)
- **整体 46%** 在 SaaS 早期是可接受水位,但 jobs / payment 这两条命脉需要专项补

## 后续优先级建议

按"业务关键 × 容易测"排序:

1. **decorators.py 27% → 70%+**(0.5h):require_credits 是扣费命脉,应有独立单测;现在只通过端点间接测过
2. **avatar.py 0% → 60%**(1h):真接口已跑通归档,加 5-6 个 happy path + error path 测试
3. **jobs.py 48% → 70%**(2h):mock fal_service 写 _execute_job 的端到端测试
4. **payment.py 50% → 70%**(1.5h):confirm_order + 退款流程
5. **admin.py 35% → 60%**(2h):管理员 force-logout / reset-model / users-list 等

总计约 7 小时 — 可作为下一轮专项任务。

## 哪些不必强补

- `main.py` / `health_check.py` / `feishu.py`:启动代码 + 外部依赖,测试 ROI 低
- `task_queue.py`:Phase 2 迁 RQ/Celery 后整体退役
- `fal_service.py`:外部 API,集成测要真 FAL key,不如用 contract test
- `circuit_breaker.py`:真触发路径罕见,留 staging 验证
