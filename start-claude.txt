你好。我每次启动你时,你都是失忆状态,所以请先重建上下文。

请按顺序做这几件事:

1. 检查当前目录,用 ls 列出文件
2. 如果有 CLAUDE.md,读它(项目说明)
3. 如果有 PROGRESS.md,读它(进度日志,重点看最后 3 条)
4. 如果有 .git,跑 git log --oneline -15 看最近 15 次提交
5. 如果有 .git,跑 git status 看当前是否有未提交改动
6. 跑 pwd 确认你在哪个项目目录

【换服务器恢复时的额外一步】
7. 检查 /root/.claude/projects/-root/memory/ 是否存在
   如果**不存在或为空**(可能换服务器后没自动恢复) → 跑:
     mkdir -p /root/.claude/projects/-root/memory
     cp -r /root/ssp/docs/memory-snapshot/* /root/.claude/projects/-root/memory/
   把 git 仓库里的项目记忆副本恢复到本地,继承上次会话的协作约定 + 项目背景

完成后,用不超过 10 句话告诉我:
- 这是什么项目(一句话)
- 上次做到哪里
- 当前是否有未完成的改动
- 你建议今天从哪里开始
- 有没有需要我决策的事

如果以上文件都不存在(CLAUDE.md / PROGRESS.md / .git 都没有),
告诉我"这是个新接手的项目,记忆为空",并问我:
"要不要先做项目考古,生成 CLAUDE.md 和 PROGRESS.md?"

调查阶段【只读不改】,不要修改任何文件,不要重启任何服务,
不要 commit。我看完你的报告再决定下一步。
